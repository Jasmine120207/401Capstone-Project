# Environment Variables Configuration

# This is an example .env file. Rename to .env and configure values.
# IMPORTANT: Never commit actual .env file to Git - it's added to .gitignore

# Server Configuration
PORT=3000
NODE_ENV=development

# Session Configuration
# IMPORTANT: Change this to a random, strong secret in production
SESSION_SECRET=your_session_secret_key_change_in_production

# Database Configuration
DB_PATH=./database.db

# For Production (if using PostgreSQL)
# Uncomment and configure these for PostgreSQL deployment
# DB_HOST=your-database-host.com
# DB_NAME=student_portal
# DB_USER=your_username
# DB_PASSWORD=your_password
# DB_PORT=5432

# Application URL for Production
# APP_URL=https://yourdomain.com

# Email Configuration (if adding email features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# Add any other environment-specific variables below
