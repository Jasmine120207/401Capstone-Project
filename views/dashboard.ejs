<%- include('./partials/header.ejs'); %>

<div class="container">
  <div class="dashboard-header">
    <div>
      <h1>Dashboard</h1>
      <p style="color: #6b7280;">Welcome back, <%= user.firstname %>!</p>
    </div>
  </div>

  <div class="welcome-section">
    <h2>👋 Welcome, <%= user.firstname %> <%= user.lastname %>!</h2>
    <p>Here's an overview of your academic profile</p>
  </div>

  <div class="grid">
    <div class="grid-item">
      <h3>📧 Email</h3>
      <p><%= user.email %></p>
    </div>
    <div class="grid-item">
      <h3>📝 Enrollment No</h3>
      <p><%= user.enrollmentNo || 'Not set' %></p>
    </div>
    <div class="grid-item">
      <h3>🏢 Department</h3>
      <p><%= user.department || 'Not set' %></p>
    </div>
    <div class="grid-item">
      <h3>📊 Semester</h3>
      <p><%= user.semester || 'Not set' %></p>
    </div>
    <div class="grid-item">
      <h3>⭐ CGPA</h3>
      <p><%= user.cgpa || 'Not set' %></p>
    </div>
    <div class="grid-item">
      <h3>📅 Member Since</h3>
      <p><%= new Date(user.createdAt).toLocaleDateString() %></p>
    </div>
  </div>

  <div class="card">
    <div class="card-header">Quick Actions</div>
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 1rem;">
      <a href="/dashboard/profile" class="btn btn-primary">Edit Profile</a>
      <a href="/auth/logout" class="btn btn-secondary" style="background-color: #ef4444;">Logout</a>
    </div>
  </div>

  <div class="card">
    <div class="card-header">Quick Tips</div>
    <ul style="margin-left: 1.5rem; line-height: 1.8;">
      <li>Keep your profile information up to date</li>
      <li>Regularly check your academic progress</li>
      <li>Monitor your CGPA and semester performance</li>
      <li>Ensure your enrollment number is correctly set</li>
    </ul>
  </div>
</div>

<%- include('./partials/footer.ejs'); %>
